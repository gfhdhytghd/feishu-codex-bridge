# 上游整合（2026-09-23）

基线：上游 `main` 的 `11ddd68`（v0.6.13 后的 Claude API 错误收尾修复）。
整合分支：`codex/upstream-union-20260923`。

| 功能 | 最终实现 |
| --- | --- |
| turn 身份过滤、steer 超时和队列竞争修复 | 上游 |
| 图片解析、上传、并发、卡片重绘 | 上游；补充 realpath 检查，阻止 qa/write 通过符号链接越出工作区 |
| 语音转写、配置控制台、转写面板和失败回退 | 上游；接入本地有序准备、取消和参与判断 |
| 运行卡片、工具详情折叠、steer 分卡 | 上游；移除旧 collapseToolCalls 重复开关 |
| Claude 错误收尾、多平台支持、会话模型设置 | 上游 |
| 群参与策略、参与价值与沉默条件、Discuss | 保留本地实现与最新保留策略修复 |
| 群历史摘要、引用历史、记忆注入与同步 | 保留本地实现 |
| 项目默认及会话 Fast 开关、继承设置 | 保留本地实现，使用上游 turn/start ACK 和身份过滤 |
| macOS Codex runtime 读取权限、显式开启预热 | 保留本地实现 |
| 全局文档评论触发策略 | 保留本地实现 |

Fast 项目默认也应用于 Discuss 首次创建的宿主会话。判断器决定 STEER 的语音先完成转写，再使用上游活动轮次与分卡入口；不会额外创建一次重复轮次。

Node 最低版本为 20.3.0（有序准备使用 AbortSignal.any）。旧的独立 ASR 实现及其配置文档已移除，语音行为以当前上游配置为准。

验证：TypeScript 类型检查、全量 Vitest 和构建。模型 live 测试默认跳过；这些检查不代表真实飞书消息、语音或图片已经完成端到端投递。
